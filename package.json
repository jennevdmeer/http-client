{
  "author": {
    "name": "Jenne van der Meer",
    "email": "jennevdmeer@impulze.net"
  },
  "description": "Basic minimal HttpClient lib, making use of old school XMLHttpRequest instead of fetch because it has better support and has more functionality.",
  "license": "ISC",
  "name": "impulze-http-client",
  "main": "dist/Index.js",
  "types": "dist/Index.d.ts",
  "version": "0.0.13",
  "scripts": {
    "build": "npm run build:types && npm run build:js",
    "build:types": "tsc --emitDeclarationOnly",
    "build:js": "babel src --out-dir dist --extensions \".js,.ts,.tsx\"",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@babel/cli": "^7.11.5",
    "@babel/core": "^7.11.5",
    "@babel/plugin-proposal-class-properties": "^7.10.4",
    "@babel/preset-env": "^7.11.5",
    "@babel/preset-typescript": "^7.10.4",
    "typescript": "^4.0.2"
  }
}
